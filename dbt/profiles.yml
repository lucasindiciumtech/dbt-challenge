dbt_challenge:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: "main"
      schema: "main"
      schemas_and_paths:
        main: "../db/database.sqlite"
      schema_directory: "../db"
